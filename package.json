{
  "name": "rust-wasm-jspi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "wasm": "cargo build --release && externref target/wasm32-unknown-unknown/release/rust_wasm_jspi.wasm -o target/wasm32-unknown-unknown/release/bin.wasm"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}